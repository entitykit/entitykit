/** Compatibility facade for relational identifier helpers. */
export {
    boundedIdentifier,
    defaultForeignKeyName,
    defaultIndexName,
    foreignKeyConstraintName,
} from '../relational-identifiers';
