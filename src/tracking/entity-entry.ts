/**
 * Public tracked-entry facade. Store recovery lives beside the entry
 * implementation while callers keep the historical module path.
 */
export {
    cloneSnapshotValue,
    EntityEntry,
} from './entity-entry-implementation';
